/**
 * StudentBoard services API
 */

export const StudentBoard = (api) => {
  const get = {
  };

  const post = {
  };

  const put = {
  };

  const patch = {
  };

  const _delete = {
  };

  return {
    get,
    post,
    put,
    patch,
    delete: _delete
  };
};

// src/services/StudentBoard.js
